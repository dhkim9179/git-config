############################################################################################### 공통 설정 시작
#actuator 설정
management:
  endpoints:
    web:
      exposure:
        include: health, env, refresh, info

server:
  port: 16082
  servlet:
    context-path: /admin
    encoding:
      charset: utf-8
      force-response: true
      
application:
  misc:
    test: 12345