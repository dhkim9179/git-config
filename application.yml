#서버 설정
server:
  shutdown: graceful

# mybatis 설정
mybatis:
  mapper-location: com.dominic
  type-alias-package: com.dominic

logging:
  charset:
    console: utf8
    file: utf8
  group:
    application: com.dominic
  level:
    application: INFO
    root: INFO

---
# Spring 설정
spring:
  config.activate.on-profile: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://172.105.223.165:3306/dominic_db
    username: root
    password: qwer1234!

application:
  redis:
    host: 172.105.223.165
    port: 6379

---
spring:
  config.activate.on-profile: dev